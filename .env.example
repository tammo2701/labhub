# Port, auf dem HomeOS im Container erreichbar ist
PORT=4283

# Node-Umgebung
NODE_ENV=production

# Zeitzone fuer Zeitstempel/Logs
TZ=Europe/Berlin

# --- Geplante Integrationen (noch nicht implementiert) ---
# DOCKER_SOCKET=/var/run/docker.sock
# BESZEL_URL=
# BESZEL_TOKEN=
